# 状态阶段 4 验收

2026-09-14：自动时间、状态保存恢复、八图标与查看入口实现完成。运行版本保持 0.4.0-alpha、core v4，存档 Schema 升为 3。模块合同见[状态生命周期与 UI](../../../status-lifecycle-ui.md)，原[执行计划](../../../plan/archive/alpha-0.4-status-lifecycle-ui-plan.md)已归档。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| Unity 6000.6.0f1 编译 | 通过，移除临时采集器后再次编译 |
| 全量 EditMode | 534/534，通过；含状态导入后装备幂等重建 |
| 长期 PlayMode | 72/72，通过；含自动计时、暂停积压与取消、死亡保存和自然复活、状态详情真实手柄输入 |
| 界面专项 | Phase1Ux 10/10，通过；中英 / Pseudo、背包与商店原有布局、详情滚动和返回 |
| 临时截图验收 | 1/1，通过；采集器已清理，不作为长期测试保留 |
| 八图标审计 | 8/8，0 Error；8 条满画布背景触边 Warning 符合不透明图标合同 |

[测试结果摘要](./test-results.json)。测试使用既有隔离夹具，没有读取或写入真实玩家存档。此前临时采集器尝试从商店 / 内容目录寻找已绑定守护状态的装备失败，改用受控调试来源采集后通过；该失败未混入长期测试通过数。退出前释放调试来源。

长期新增及扩展覆盖：独立周期相位、多次保存往返不漂移、抵抗数据与最强候选保留、失效来源无新角色权限、未发布 / 积压边界、来源恢复提供方、真实 Schema 2 迁移、暂停与 Session 取消、装备状态不重复叠层、死亡恢复 / 自然复活、鼠标悬停与手柄进入详情 / 滚动 / 返回、驱散后的焦点归位。复用原状态、存档、生命周期和 UI 套件，无版本专属长期套件。

## 可见界面

- [中文 1920×1080](./status-zh-Hans.png)
- [英文 1920×1080](./status-en.png)
- [Pseudo 1920×1080](./status-pseudo.png)
- [中文 1280×720](./status-small.png)

八图标、层数、计时、玩家 / 怪物列表和详情均可见。长详情使用滚动区域，确认进入、返回先回图标；浮窗优先位于窗口外侧，避免盖住图标与关闭按钮。顶部栏保持原高度，通过文字换行与内边距适配扩张文本；状态关闭按钮按文本宽度伸展。Pseudo 为临时文本扩张压力检查，动态状态内容在刷新后回到英文；此前已检查过扩张后的逐层详情布局。

本轮可见证据来自 Editor PlayMode，未宣称独立 Windows Player 验收通过。Player 可见交互、综合验收矩阵及运行版本封板合并到总计划阶段 5；状态完成后再实现统一可使用物品与消耗品。

## 资源与收尾

八个独立 PNG 经 ImageGen 逐枚生成、逐枚审计，统一完整画布导出；没有生产图集或手动切片。见[资产合同](./asset-contract.md)、[原始提示](./generation-prompts.md)、[审计结果](./sprite-audit.json)。通过 Unity MCP 导入 Sprite Single / PPU64，并绑定正式 StatusDefinition.Icon 和 Session Addressables 组。

UI 布局通过 Unity MCP 创建和调整，复用现有 PanelRenderer。没有场景或 Prefab YAML 修改。清理了临时截图测试，保留历史存档夹具；恢复测试生成的中文字体缓存和 Addressables 设置自动序列化噪声。本轮没有 Git 提交或发布构建。
