# 状态阶段 3 验收

日期：2026-09-13。Unity 6000.6.0f1，基线提交 `ce52094`。运行版本保持 0.4.0-alpha；正式内容 core v4，Schema 2。

## 结果

- [EditMode 全量](./editmode.json)：527/527 通过，包含七异常、抵抗、来源、原格位回退、旧档兼容及原有模块回归。
- [本地化专项](./localization.json)：补齐 statuses 启动 / 切换预载后的 20/20 通过，包括名称、说明、中英 / Pseudo 字符及字体覆盖。
- [PlayMode 全量](./playmode.json)：68/68 通过，包含正式眩晕 50% 抵抗后持续 1 秒、移动停止及到期恢复、原有输入、Session 和保存恢复流程。
- 编译无错误；测试未新增 Ignore 或删除独有覆盖。阶段新增用例集中在原有状态战斗、存档和本地化套件。

## 实现与边界

七异常与七项抗性属性、词缀、内容目录及中英配置已落地。统一来源支持技能直接施加 / 驱散、投射物命中附加、装备维持、消费标记换取临时效果。Odin 调试菜单为 `DarkFlare/调试/状态系统`。

默认技能与物品不主动附加异常 / 光环；`status_burning_example` 和 `equipment_guard` 供显式启用验证。装备样例由测试克隆物品配置后走真实穿脱入口，未为样例复制正式物品图标。具体幅度和时长为首期可调参数，详见[模块](../../../status-ailments.md)。

core v3 → v4 为追加内容兼容，保留历史实例掷值和源 DTO，旧档新抗性缺省为 0；v1 / v2 旧链继续可用。尚无状态实例存档或自动 Session 时间，图标与焦点 UI 留给阶段 4；完整消耗品在状态系统验收后实现。

## 验证中修复

初次专项发现来源修改器为空时的周期伤害异常，已统一为空集合。全量回归发现 HUD 固定属性清单、旧词缀池数量与新增资源分组 / 池覆盖问题，已同步接入。旧资源和词缀测试以完整集合覆盖替代历史固定数量；标签迁移矩阵仍精确验证原历史集合。装备失败回退增加原格位断言；补齐本地化预载后同步职责表合同，最终全部通过。

一次性配置创建脚本和轮询输出在验收后清理；保留本目录的当次结果，不创建读取历史成功日志的运行测试。本轮没有提交 Git。
