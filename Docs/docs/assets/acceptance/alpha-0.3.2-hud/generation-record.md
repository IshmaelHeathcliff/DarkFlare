# HUD 底板生成记录

2026-09-09，使用 imagegen 编辑生成，参考为已批准且未修改的 `Assets/Art/Sprites/UI/Frames/ui_item_window_base.png`。按[资产合同](./asset-contract.md)生成独立资源仪表底板。

生成约束：1254×1254 固定完整画布，一个不带文字、图标或资源填充的中性面板；炭黑低对比内底、硬边旧铁轮廓、少量暗金铜角件；全出血不透明；角部独特装饰限定在 88×88，96 px 九宫格切线不穿独特元素；长边与中央可安全拉伸，不使用组件拼图或人工裁切。

第一次输出 `exec-cccf9022-8f07-4873-b042-567994275b4e.png`：角部台阶延伸到约 115 源像素，越过保护区，拒绝导入。第二次明确缩短角件，使其收在约 72 源像素内；输出 `exec-8f399732-2de5-4de7-a91b-c8344aed942d.png` 通过检查，原图直接作为 `ui_hud_resource_base.png` 导入。没有手工修图、重采样或切片。

自动审计见 [panel-audit.json](./panel-audit.json)：0 错误，1 项预期提示（可见内容接触画布边缘）。合同明确全出血，此提示不表示被裁切。原生中心约 (627,627)，画布与覆盖符合合同；实际 300×136 与 360×136 验证角件稳定、长边无独特纹理变形。

生命与法力复用同一中性底板，颜色与 0–100% 比例由独立 ProgressBar 表达，未生产无必要的重复图片。输入图标复用现有家族。
