# 阶段 3：随机化与掉落规则执行记录

## 状态

- 状态：已完成并归档
- 开始日期：2026-08-06
- 完成日期：2026-08-06
- 目标场景：`Assets/Scenes/Main.unity`
- 模块说明：[随机化与掉落规则](../../randomization-system.md)

## 完成内容

1. 新增 `GameplayRandomSystem`，按生成位置、怪物实例、玩家攻击、怪物攻击和掉落维护独立序列。
2. `CombatPrototypeBootstrap` 增加固定种子调试入口，默认关闭并记录根种子。
3. 新增 `MonsterInstanceData`，怪物生命按 `0.85–1.15` 倍率生成且不修改共享资产。
4. 玩家与怪物攻击改用统一种子链，正式配置分别为 `10–14` 与 `6–10`。
5. `LootTableDefinition` 增加表级掉落概率，基础掉落表配置为 `35%`；概率与条目权重保持分离。
6. 掉落失败合法返回 `null`，掉落实例 ID、物品种子和日志由掉落种子稳定生成。
7. 增加生命、伤害和掉落配置校验，并补充确定性、范围、边界概率、统计容差和正式资产测试。

## 验收结果

- Runtime、Editor、EditMode 与 PlayMode 四个程序集编译通过，0 警告、0 错误。
- Unity EditMode 全量 82/82 通过。
- Unity PlayMode 共 11 项：9 项通过、2 项为项目既有 Ignore、0 失败。
- `0%` / `100%` 掉落边界通过；固定种子 `35%` 的 10000 次模拟结果位于 `32%–38%` 容差内。
- 固定种子 `24681357` 连续运行两次 `Main`，前三只怪物实例种子与生命、三次玩家攻击、三次怪物攻击和三次掉落结果逐项一致。
- 关闭固定种子后连续加载两次 `Main`，根种子正常变化；场景固定种子开关保持默认关闭。
- Unity Console 未出现本阶段新增的编译错误、断言失败或运行时异常，代码差异检查通过。

## 完成边界

- 阶段 3 只完成随机规则和现有单怪物配置。
- 三种怪物、七件装备、标签和词条池扩充留在阶段 4。
- 暴击、命中、闪避、保底和多次掉落不在本阶段范围。
